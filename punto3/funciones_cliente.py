import json

def menu(cadena):
    lista = json.loads(cadena)
    for i in lista:
        print i

def comprar_productos(cadena):
    lista = json.loads(lista)
    for j in lista:
        print j

def ventas_dia(cadena):
    lista = json.loads(lista)
    for k in lista:
        print k

def inventarios(cadena):
    lista = json.loads(lista)
    for l in lista:
        print l
